[project]
name = "depfresh"
version = "0.1.0"
description = "Check if pyproject.toml dependencies are up to date with PyPI — no install required"
authors = [
    { name = "thejedi433", email = "thejedi433@outlook.com" }
]
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "packaging>=24.0",
]

[project.scripts]
depfresh = "depfresh:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "pytest-httpx>=0.30.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.coverage.run]
source = ["depfresh"]
branch = true
omit = ["tests/*", "src/depfresh/__main__.py"]

[tool.coverage.report]
fail_under = 100
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if __name__",
]
